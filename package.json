{
  "name": "@terraeclipse/bundler",
  "version": "0.0.0",
  "description": "construct an app using a set of re-usable code bundles",
  "main": "lib/index.js",
  "scripts": {
    "build": "./node_modules/.bin/babel src -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-polyfill": "^6.7.4",
    "babel-preset-es2015": "^6.6.0"
  },
  "dependencies": {
    "n-deep-merge": "0.0.1"
  }
}
